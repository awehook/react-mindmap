version: "3.9.1"
services:
  backend: 
    build: backend
    ports: 
      - "5000:5001"
    environment:
      EVERNOTE_TOKEN: "${EVERNOTE_TOKEN}"
  frontend:
    build: frontend
    ports: 
      - "8080:8080"
    environment:
      EVERNOTE_API_ENDPOINT: "${EVERNOTE_API_ENDPOINT}"